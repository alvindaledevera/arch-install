# arch-install
run the code: 

command -v git >/dev/null || pacman -Sy --noconfirm git; git clone https://github.com/alvindaledevera/arch-install.git; cd arch-install; chmod +x install.sh; ./install.sh



or

nocorrect pacman -Sy --noconfirm git && git clone https://github.com/alvindaledevera/arch-install.git && cd arch-install && chmod +x install.sh && ./install.sh

